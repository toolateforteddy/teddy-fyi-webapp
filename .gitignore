# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# vite-plugin-pwa's dev-mode output. devOptions is off in
# apps/grocery/vite.config.ts, so this should never appear -- listed so that
# turning it on for an afternoon's debugging cannot commit a service worker.
dev-dist

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
