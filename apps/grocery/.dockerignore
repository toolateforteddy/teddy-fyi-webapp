# The image is nginx, its configuration and the fetch script. The app itself is no
# longer built into it at all -- it is a release asset the init container pulls at
# startup -- so `dist` is listed here to keep a local build out of the context rather
# than because a COPY might pick it up.
node_modules
dist
src
public
icons
tests
k8s
*.json
*.ts
*.html
.dockerignore
