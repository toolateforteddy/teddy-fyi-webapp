# Repository Rules

This repository uses `pnpm` for package management and script execution. When executing commands in this repository, follow these rules:

- **Running Tests**: Always use `pnpm test:run`
- **Linting**: Always use `pnpm lint`
- **Building**: Always use `pnpm build`

Do not use `npm run` or other package managers.
